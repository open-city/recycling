<a href="/" class="start-over close-dialog fa fa-close"></a>

<p id="searchResultsFormLabel"><%= buildingsFoundMessage %></p>
<ul>
  <% for (var i=0; i<possibleAddresses.length; i++) { %>
  <% var addr = possibleAddresses[i] %>
  <% var lat = addr.lat; %>
  <% var lng = addr.lon; %>
  <li class='search-result'>
    <a class="clickable"
       href='/locations.json?latitude=<%= lat %>&longitude=<%= lng %>'
       data-latitude='<%= lat %>'
       data-longitude='<%= lng %>'
    >
      <%= addr.display_name %>
    </a>
  </li>
  <% } %>
</ul>

<p>
  <a class='btn btn-default btn-block start-over' href='/'>Start Over</a>
</p>
